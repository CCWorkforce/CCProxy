# Core dependencies for CCProxy
# Pinned versions for reproducible builds

# FastAPI framework
fastapi>=0.119.0
uvicorn[standard]>=0.37.0
orjson>=3.11.3

# OpenAI client with aiohttp support for better performance
openai[aiohttp]>=2.3.0
httpx-aiohttp>=0.1.8

# Data validation
pydantic>=2.12.0
pydantic-settings>=2.11.0

# Token counting
tiktoken>=0.12.0
tqdm>=4.67.1
typer>=0.19.2
typing-extensions>=4.15.0
typing-inspection>=0.4.2
urllib3>=2.5.0
uvloop>=0.21.0
watchfiles>=1.1.0

# HTTP client with connection pooling and HTTP/2 support
httpx[http2]>=0.28.1

# Alternative high-performance HTTP client (optional)
aiohttp>=3.13.0
aiodns>=3.5.0
yarl>=1.22.0

# Python dotenv for environment variables
python-dotenv>=1.1.1

# Async file I/O
aiofiles>=25.1.0

# OpenTelemetry for distributed tracing
opentelemetry-api>=1.37.0
opentelemetry-sdk>=1.37.0
opentelemetry-exporter-otlp-proto-grpc>=1.37.0
opentelemetry-exporter-jaeger-thrift>=1.21.0

# Production server (optional, for better performance)
locust==2.41.5
starlette>=0.48.0
standard>=1.0.3
asyncio>=4.0.0

# Async utilities for bridging sync/async code
asyncer>=0.0.9
anyio>=4.11.0

# System monitoring for thread pool management
psutil>=7.1.0
uvloop>=0.21.0
httptools>=0.7.1
